var MG = require('basic');
var baseURL = MG.BASE_URL;
module.exports = {
/* return a Promse object(axios.post / other method)
	getVideoDetail:function (param) {       
		var data = {}
		data.service = 'videoInfoProvider';
		data.method = 'getVideoInfoById';
		data.data = param;
		return axios.post(baseURL, data);
	}
	*/
}